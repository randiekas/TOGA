<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.sakolah.guru.toga_guru">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Dark">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Dashboard" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".BeritaSekolah" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".BeritaSekolahDetail" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".Jadwal" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".TanyaJawab" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".GrupKelas" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".NilaiSiswa" android:theme="@style/MyMaterialTheme" />
        <activity android:name=".MainActivity" android:theme="@style/AppTheme" />
    </application>

</manifest>